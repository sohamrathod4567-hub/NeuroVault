# Server Port
PORT=3000

# JSON Web Token Secret (Change this in production)
JWT_SECRET=super_secret_production_key_here

# Node Environment (development | production)
NODE_ENV=production

# Note: NeuroVault uses 100% free, local/proxy AI models for RAG and embeddings.
# No OpenAI or HuggingFace API keys are required to run this application!
